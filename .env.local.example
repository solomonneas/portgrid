# Data source: librenms (default) or netdisco
DATA_SOURCE=librenms

# LibreNMS (required if DATA_SOURCE=librenms)
LIBRENMS_URL=https://your-librenms-instance.com
LIBRENMS_API_TOKEN=your-api-token-here

# NetDisco (required if DATA_SOURCE=netdisco)
NETDISCO_URL=https://your-netdisco-instance.com
NETDISCO_API_KEY=your-api-key-here

# Device filtering (optional)
# Comma-separated patterns to EXCLUDE devices. Supports wildcards (*).
# Matches against hostname OR IP address.
# Examples:
#   DEVICE_EXCLUDE=10.2.50.*              # Exclude all 10.2.50.x IPs
#   DEVICE_EXCLUDE=*.vm.local,10.2.50.*   # Exclude VMs by hostname or IP
#   DEVICE_EXCLUDE=server01,server02      # Exclude specific hostnames
DEVICE_EXCLUDE=

# Comma-separated patterns to INCLUDE devices (whitelist mode).
# If set, ONLY matching devices are shown. Supports wildcards (*).
# Examples:
#   DEVICE_INCLUDE=switch*,10.1.*         # Only show switches and 10.1.x network
DEVICE_INCLUDE=
